syntax = "proto3";

package vault;

service Vault {
    rpc CreateVault (VaultRequest) returns (VaultReply);
}

message VaultRequest {
   string name = 1;
}

message VaultReply {
    string message = 1;
}